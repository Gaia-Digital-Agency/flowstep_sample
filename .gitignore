# deps
node_modules
.pnpm-store

# build output
dist
build
.next
.vite
.turbo
.payload

# server-only — generated/applied during deploy on gda-s01
packages/cms/src/migrations/

# env
.env
.env.*
!.env.example

# logs
logs
*.log
npm-debug.log*
pnpm-debug.log*

# editor / OS
.DS_Store
._*
.idea
.vscode/*
!.vscode/extensions.json

# typescript
*.tsbuildinfo

# test
coverage

# uploads (payload media)
packages/cms/media
packages/cms/uploads

# references — keep prompt + PDF + Figma source code, exclude heavy PNGs
references/**/*.png
