# Gaia AI Prompt Test Site — Desktop Home Page

This is a code bundle for Gaia AI Prompt Test Site — Desktop Home Page. It's based on default Vite project. The original design is available at https://app.flowstep.ai/file?activeFileId=0c9a34cc-1433-4887-b859-e5fa1c7fd54c.

## Running the code

Run `npm i` and `npm run setup` to install the dependencies.

Run `npm run dev` to start the development server.
